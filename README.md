# MSWUnPacker

Converts Apex Legends .msw shader files to season 3 (r5valkyrie/r5reloaded) version.

use:

mswunpacker.exe convert-legacy inputfolderpath outputfolderpath

currently it only supports up to s9 shaders (wip)